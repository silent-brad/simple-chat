version     = "0.1.0"
author      = "User"
description = "Simple Chat"
license     = "MIT"

requires "nim >= 2.0.0"
requires "karax >= 1.3.0"
requires "jester >= 0.5.0"
requires "ws >= 0.5.0"

bin = @["index"]

srcDir = "."
